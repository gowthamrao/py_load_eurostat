# ===================================================================
# Example Environment Variables for py_load_eurostat
# ===================================================================
#
# Copy this file to .env and fill in the values for your environment.
# These variables configure the database connection and other settings.

# --- Database Settings ---
# The type of database to connect to. "postgres" is recommended.
# Options: "postgres", "sqlite"
PY_LOAD_EUROSTAT_DB_TYPE="postgres"

# The host of your PostgreSQL server.
PY_LOAD_EUROSTAT_DB__HOST="localhost"

# The port of your PostgreSQL server.
PY_LOAD_EUROSTAT_DB__PORT="5432"

# The name of the database to use.
PY_LOAD_EUROSTAT_DB__NAME="eurostat"

# The username for connecting to the database.
PY_LOAD_EUROSTAT_DB__USER="postgres"

# The password for the database user. This should be set securely.
# For local development, you might use a simple password.
PY_LOAD_EUROSTAT_DB__PASSWORD="mysecretpassword"


# --- Optional Settings ---

# Logging level for the application.
# Options: "DEBUG", "INFO", "WARNING", "ERROR"
# PY_LOAD_EUROSTAT_LOG__LEVEL="INFO"

# Path to the cache directory for downloaded files.
# Defaults to ~/.cache/py-load-eurostat if not set.
# PY_LOAD_EUROSTAT_CACHE__PATH="/path/to/your/cache"

# For PostgreSQL, setting this to "false" will use standard tables for
# staging instead of UNLOGGED tables. UNLOGGED is faster but not crash-safe.
# PY_LOAD_EUROSTAT_DB__USE_UNLOGGED_TABLES="true"
